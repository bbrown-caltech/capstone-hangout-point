apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fist.fullname" . }}-test-connection"
  labels:
    {{- include "fist.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fist.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
